# lerobot_ros2 运行时环境配置
# init.sh 会读取此文件；个人覆盖可建 .fa-env.local.toml（已 gitignore）
# 临时覆盖: export LR_ENV_BACKEND=uv

backend = "uv"

[conda]
name = "lerobot-ros2"

[uv]
venv = ".venv"

[lerobot]
version = "0.5.1"

[ros2]
# workspace = "~/ros2_ws"
