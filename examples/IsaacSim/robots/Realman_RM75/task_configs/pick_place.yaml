# Realman RM75 — pick / place（skill_defaults / skill_params 分层）

task_key: pick_place
label: Pick Place
use_stamped: false
robot_id: realman_rm75_pick_place
default_scene: default

task_queue:
  - skill: env.randomize_object_local_xyz
  - skill: robot.cache_ee_pose
    params:
      key: rm75_prep_pose
  - skill: single_arm.pick
  - skill: single_arm.place
  - skill: single_arm.goto_cache_pose
    params:
      key: rm75_prep_pose

base_task_overrides:
  max_stage_duration: 2.0
  pose_tol_pos: 0.025
  pose_tol_ori: 0.08
  require_orientation_reach: false
  use_object_orientation: false

skill_defaults:
  env.randomize_object_local_xyz:
    object_prim_path: /World/banana/banana/banana
    xyz_offset: [0.15, 0.15, 0.0]
  single_arm.pick:
    arm: left
    prepare_offset: [0.0, 0.0, 0.3]
    object_prim_path: /World/banana/banana/banana
    ee_base_orientation: [0.0, 1.0, 0.0, 0.0]
    grasp_direction: +z
    grasp_direction_vector: [0, 0, 1]
  single_arm.place:
    place_position: [0.39015462527214956, -0.27559945312297013, 0.44]
    ee_base_orientation: [0.0, 0.8, 0.0, 0.6]
    ee_retreat_offset: [0.0, 0.2, 0.0]

scene_presets:
  default: {}

record:
  base_record_overrides:
    task_name: pick_place
    fps: 30
    camera_info_timeout: 10.0
    enable_keypoint_pcd: false
    include_depth_feature: false
    image_writer_processes: 0
    image_writer_threads: 8
    video_encoding_batch_size: 5
  profiles:
    - key: default
      label: Default
      overrides: {}
    - key: fast
      label: Fast Encode Queue
      overrides:
        image_writer_threads: 12
        video_encoding_batch_size: 10
