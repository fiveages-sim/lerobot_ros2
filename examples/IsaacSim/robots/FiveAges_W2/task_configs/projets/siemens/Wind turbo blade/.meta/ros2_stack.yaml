# Scenario ROS2 stack overlay (not a task orchestration file).
# Merged over robot.yaml ros2_stack; do not add task_key / task_queue here.
motion:
  preset: ocs2-fullbody
  args:
    type: rg75
navigation:
  profile: map_only
  args:
    map: wind_turbo_task1
