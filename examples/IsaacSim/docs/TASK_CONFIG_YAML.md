# 任务配置：YAML 说明

完整说明已迁至 **`robot_action_composer`** 子模块（与代码同源，避免重复维护）：

- **[任务 YAML 格式与示例路径](../../../submodules/robot_action_composer/docs/TASK_CONFIG_YAML.md)**
- **[机器人目录与 `robot_config.py`](../../../submodules/robot_action_composer/docs/ROBOT_CONFIG.md)**

包内总览：**[README（含架构与设计）](../../../submodules/robot_action_composer/README.md)**。

`task_queue`、并行块、`handover` 收尾（`robot.cache_ee_pose` + `dual_arm.goto_cache_pose`）等见子模块 **`robot_action_composer/docs/TASK_CONFIG_YAML.md`** 中 **「task_queue 与技能名」** 一节。
